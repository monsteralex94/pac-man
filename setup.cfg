[metadata]
name = pac_man
version = 1.0.0
license = GPL-3.0

[options]
packages = find:
package_dir =
        = src
include_package_data = True
install_requires = file: requirements.txt

[options.packages.find]
where = src